version: '3.8'
services:
  client:
    image: client-prod-i
    build:
      dockerfile: Dockerfile.prod
    container_name: client-prod-c
    env_file: .env
    volumes:
      - ./client/certs:/etc/ssl/private:ro
    ports:
      - "8081:443"